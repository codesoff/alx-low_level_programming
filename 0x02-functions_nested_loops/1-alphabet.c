#include "main.h"
/**
 * main - print_alphabet 
 * prints alphabet,in lowercase
 * Return: no return
 */
void print_alphabet(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
	_putchar(ch);
_putchar('\n');
}
