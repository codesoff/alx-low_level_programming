#include "main.h"
/**
 * print_alphabet 
 * prints alphabet,in lowercase
 * Return: no return
 */
int main(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
	_putchar(ch);
_putchar('\n');
return (0);
}
